import SecurityCode from './../../../../src/core/classes/validation/SecurityCode.class';

// given
describe('Class Security Code', () => {
  const fieldId = 'security-code';
  let securityCodeInstance = new SecurityCode(fieldId);

  // when
  beforeEach(() => {});
  // given
  describe('Method inputValidationListener', () => {
    // when
    beforeEach(() => {});
  });

  // given
  describe('Method postMessageEventListener', () => {
    // when
    beforeEach(() => {});
  });

  // given
  describe('Method setSecurityCodeProperties', () => {
    // when
    beforeEach(() => {});
  });

  // given
  describe('Method cardNumberSecurityCodeMatch', () => {
    // when
    beforeEach(() => {});
  });
});
