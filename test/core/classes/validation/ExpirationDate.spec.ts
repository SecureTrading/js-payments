// given
describe('ExpirationDate class', () => {
  // given
  describe('dateInputMask method', () => {
    beforeEach(() => {});
    // then
    it('should prevent enter char except digit', () => {});

    // then
    it('should indicate slash at third place', () => {});
  });
});
