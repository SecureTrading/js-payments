import './style.scss';
import { inputListener } from '../../../src/core/listeners/inputListener';

inputListener(
  'expirationDate',
  'st-expiration-date',
  'expiration-date',
  'expiration-date'
);
