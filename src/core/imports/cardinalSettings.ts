const loggingConfiguration = { logging: { level: 'on' } };

const cardinalCommerceConfig = {
  loggingConfiguration
};

const GATEWAY_URL = 'https://webservices.securetrading.net/jwt/';

export { cardinalCommerceConfig, GATEWAY_URL };
